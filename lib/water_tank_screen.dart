import 'package:flutter/material.dart';

class WaterTankScreen extends StatelessWidget {
  const WaterTankScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Water Tank Screen'));
  }
}
