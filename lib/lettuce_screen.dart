import 'package:flutter/material.dart';

class LettuceScreen extends StatelessWidget {
  const LettuceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Lettuce Screen'));
  }
}
