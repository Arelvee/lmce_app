import 'package:flutter/material.dart';

class WebAppScreen extends StatelessWidget {
  const WebAppScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Web App Screen'));
  }
}
